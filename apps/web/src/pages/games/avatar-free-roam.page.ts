import { supabase } from '../../core/supabase/client';
import './avatar-free-roam.css';

type Vec={x:number;y:number};type Enemy={id:number;x:number;y:number;hp:number;max:number;speed:number;boss?:boolean};type Loot={x:number;y:number;kind:string};
const GAME_ID='f4cfc366-a4bf-4b8d-b4cb-755da293711f',SAVE='rb-afr-v1';
const app=document.querySelector<HTMLElement>('#app')!;
app.innerHTML=`<main class="afr-shell"><div class="afr-wrap"><header class="afr-top"><a href="/gaming.html">← GAMING HQ</a><div class="afr-brand"><small>ELITE RPG 01 / 04</small><h1>AVATAR FREE ROAM</h1></div><span class="afr-status" id="net">OFFLINE WORLD READY</span></header><section class="afr-hero"><div><small class="afr-kicker">OPEN WORLD • MISSIONS • COMBAT • LOOT • PORTALS • REALTIME • OFFLINE</small><h2>OWN THE<br>UNIVERSE.</h2><p>Control your Rich Bizness character across a living portal city. Explore districts, fight smoke creatures, complete missions, collect elite gear, upgrade powers, enter portals, and synchronize progress with Gaming, Avatar and Meta.</p></div><div class="afr-stats"><article><small>RICH XP</small><strong id="xp">0</strong></article><article><small>LEVEL</small><strong id="level">1</strong></article><article><small>MISSIONS</small><strong id="missions">0</strong></article><article><small>BEST SCORE</small><strong id="best">0</strong></article></div></section><section class="afr-grid"><div class="afr-stage"><div class="afr-hud"><div class="afr-hud-box"><b id="name">PORTAL RONIN</b><div class="afr-bar"><i id="hp"></i></div><small id="healthText">100 / 100 HP</small></div><div class="afr-zone" id="zone">PORTAL PLAZA</div><div class="afr-hud-box"><b id="questHud">MISSION 1</b><div class="afr-bar"><i id="energy"></i></div><small id="energyText">100 ENERGY</small></div></div><canvas id="world" width="1280" height="800" aria-label="Avatar Free Roam game world"></canvas><div class="afr-controls"><button class="afr-btn" data-act="left">LEFT</button><button class="afr-btn" data-act="up">UP</button><button class="afr-btn" data-act="down">DOWN</button><button class="afr-btn" data-act="right">RIGHT</button><button class="afr-btn" data-act="attack">STRIKE</button><button class="afr-btn" data-act="dash">DASH</button><button class="afr-btn primary" data-act="power">AURA</button></div></div><aside class="afr-side"><section class="afr-card"><h3>WORLD COMMAND</h3><select class="afr-select" id="mode"><option value="campaign">STORY CAMPAIGN</option><option value="survival">VOID SURVIVAL</option><option value="local">LOCAL SCORE HUNT</option><option value="online">REALTIME EXPEDITION</option></select><select class="afr-select" id="difficulty"><option value="1">ROOKIE WORLD</option><option value="2" selected>ELITE WORLD</option><option value="3">BOSS WORLD</option></select><button class="afr-btn primary" id="start">ENTER WORLD</button><p class="afr-log" id="log">Choose a mode and enter the portal city.</p></section><section class="afr-card"><h3>ACTIVE MISSIONS</h3><div id="quests"></div></section><section class="afr-card"><h3>GEAR & POWERS</h3><div id="inventory"></div></section><section class="afr-card"><h3>REALTIME PARTY</h3><input class="afr-input" id="roomCode" maxlength="8" placeholder="ROOM CODE"/><div class="afr-row"><button class="afr-btn primary" id="createRoom">CREATE</button><button class="afr-btn" id="joinRoom">JOIN</button></div><p class="afr-log" id="roomState">Tap In required for realtime expeditions.</p></section><section class="afr-card"><h3>WORLD LEADERBOARD</h3><div id="leaders"></div></section></aside></section><footer class="afr-footer">AVATAR FREE ROAM v1.0 • ORIGINAL RPG WORLD • GAMING + AVATAR + META SYNC</footer></div></main>`;
const $=<T extends HTMLElement>(q:string)=>document.querySelector<T>(q)!;const canvas=$<HTMLCanvasElement>('#world'),ctx=canvas.getContext('2d')!;
let user:any=null,profile:any=null,room:any=null,channel:any=null,running=false,mode='campaign',difficulty=2,started=Date.now(),last=0,score=0,best=0,xp=0,level=1,mission=1,missionsDone=0,energy=100,hp=100,maxHp=100,coins=0,kills=0,portalShards=0,keys=new Set<string>(),enemies:Enemy[]=[],loot:Loot[]=[],particles:{x:number;y:number;r:number;life:number}[]=[],worldTime=0;
const p={x:640,y:400,r:18,speed:220,dir:0},camera={x:0,y:0},world={w:2600,h:1800};const upgrades=new Set<string>();
const zones=[{n:'PORTAL PLAZA',x:0,y:0,w:850,h:620,c:'#0a2112'},{n:'SMOKE ALLEY',x:850,y:0,w:900,h:620,c:'#15121d'},{n:'GOLD MARKET',x:1750,y:0,w:850,h:620,c:'#211b09'},{n:'META GARDENS',x:0,y:620,w:1000,h:1180,c:'#092019'},{n:'VOID DISTRICT',x:1000,y:620,w:900,h:1180,c:'#130b1b'},{n:'BOSS TOWER',x:1900,y:620,w:700,h:1180,c:'#201008'}];
const quests=()=>[{t:'Clear Smoke Creatures',v:kills,g:8+mission*2},{t:'Collect Portal Shards',v:portalShards,g:3+Math.floor(mission/2)},{t:'Reach the Boss Tower',v:p.x>1900&&p.y>620?1:0,g:1}];
function toast(t:string){const e=document.createElement('div');e.className='afr-toast';e.textContent=t;document.body.append(e);setTimeout(()=>e.remove(),1300)}
function save(){localStorage.setItem(SAVE,JSON.stringify({score,best,xp,level,mission,missionsDone,coins,portalShards,upgrades:[...upgrades],x:p.x,y:p.y}));}
function load(){try{const s=JSON.parse(localStorage.getItem(SAVE)||'{}');score=s.score||0;best=s.best||0;xp=s.xp||0;level=s.level||1;mission=s.mission||1;missionsDone=s.missionsDone||0;coins=s.coins||0;portalShards=s.portalShards||0;(s.upgrades||[]).forEach((x:string)=>upgrades.add(x));p.x=s.x||640;p.y=s.y||400;}catch{}renderSide();syncHud()}
function renderSide(){const q=quests();$('#quests').innerHTML=q.map(x=>`<div class="afr-quest"><div><b>${x.t}</b><small>${Math.min(x.v,x.g)} / ${x.g}</small></div><span>${x.v>=x.g?'✓':'ACTIVE'}</span></div>`).join('');const items=[['armor','RICH ARMOR',4],['dash','PHASE DASH',4],['aura','OMEGA AURA',6],['magnet','LOOT MAGNET',5]];$('#inventory').innerHTML=items.map(([k,n,c])=>`<div class="afr-item"><div><b>${n}</b><small>${upgrades.has(k)?'EQUIPPED':`${c} SHARDS`}</small></div><button class="afr-btn" data-up="${k}" data-cost="${c}" ${upgrades.has(k)?'disabled':''}>${upgrades.has(k)?'✓':'UNLOCK'}</button></div>`).join('')+`<p class="afr-log">Coins: <b>${coins}</b> • Shards: <b>${portalShards}</b></p>`;document.querySelectorAll<HTMLButtonElement>('[data-up]').forEach(b=>b.onclick=()=>{const c=Number(b.dataset.cost);if(portalShards<c)return toast('MORE PORTAL SHARDS NEEDED');portalShards-=c;upgrades.add(b.dataset.up!);save();renderSide();});}
function syncHud(){maxHp=100+(upgrades.has('armor')?35:0)+level*3;hp=Math.min(hp,maxHp);$('#hp').style.width=`${hp/maxHp*100}%`;$('#energy').style.width=`${energy}%`;$('#healthText').textContent=`${Math.ceil(hp)} / ${maxHp} HP`;$('#energyText').textContent=`${Math.ceil(energy)} ENERGY`;$('#xp').textContent=String(xp);$('#level').textContent=String(level);$('#missions').textContent=String(missionsDone);$('#best').textContent=String(Math.max(best,score));$('#questHud').textContent=`MISSION ${mission}`;const z=zones.find(z=>p.x>=z.x&&p.x<z.x+z.w&&p.y>=z.y&&p.y<z.y+z.h);$('#zone').textContent=z?.n||'PORTAL WILDS';}
function spawnWave(){enemies=[];const count=6+mission*2+difficulty*2;for(let i=0;i<count;i++){const a=Math.random()*Math.PI*2,d=420+Math.random()*500;enemies.push({id:i,x:Math.max(30,Math.min(world.w-30,p.x+Math.cos(a)*d)),y:Math.max(30,Math.min(world.h-30,p.y+Math.sin(a)*d)),hp:35+difficulty*12+mission*4,max:35+difficulty*12+mission*4,speed:45+difficulty*14,boss:false})}enemies.push({id:999,x:2250,y:1200,hp:220+mission*35,max:220+mission*35,speed:36+difficulty*8,boss:true});}
function start(){mode=($('#mode') as HTMLSelectElement).value;difficulty=Number(($('#difficulty') as HTMLSelectElement).value);running=true;started=Date.now();hp=maxHp;energy=100;kills=0;portalShards=0;spawnWave();$('#log').textContent=`MISSION ${mission} • ${mode.toUpperCase()} • Explore, fight and complete all objectives.`;requestAnimationFrame(loop)}
function attack(power=false){if(!running)return;const cost=power?38:5;if(energy<cost)return toast('ENERGY LOW');energy-=cost;const range=power?160:72,damage=(power?58:22)+level*3+(upgrades.has('aura')&&power?35:0);for(const e of enemies){const d=Math.hypot(e.x-p.x,e.y-p.y);if(d<range){e.hp-=damage;for(let i=0;i<8;i++)particles.push({x:e.x,y:e.y,r:3+Math.random()*8,life:1});if(e.hp<=0){kills++;score+=e.boss?2200:180+mission*30;coins+=e.boss?60:5;loot.push({x:e.x,y:e.y,kind:Math.random()>.6?'shard':'coin'});}}}enemies=enemies.filter(e=>e.hp>0);syncHud();renderSide()}
function dash(){if(energy<18)return;energy-=18;const boost=upgrades.has('dash')?190:120;p.x+=Math.cos(p.dir)*boost;p.y+=Math.sin(p.dir)*boost;p.x=Math.max(20,Math.min(world.w-20,p.x));p.y=Math.max(20,Math.min(world.h-20,p.y));}
function update(dt:number){worldTime+=dt;let dx=0,dy=0;if(keys.has('arrowleft')||keys.has('a'))dx--;if(keys.has('arrowright')||keys.has('d'))dx++;if(keys.has('arrowup')||keys.has('w'))dy--;if(keys.has('arrowdown')||keys.has('s'))dy++;if(dx||dy){const len=Math.hypot(dx,dy);dx/=len;dy/=len;p.dir=Math.atan2(dy,dx);p.x+=dx*p.speed*dt;p.y+=dy*p.speed*dt;}p.x=Math.max(20,Math.min(world.w-20,p.x));p.y=Math.max(20,Math.min(world.h-20,p.y));energy=Math.min(100,energy+8*dt);for(const e of enemies){const d=Math.hypot(p.x-e.x,p.y-e.y);if(d<500){e.x+=(p.x-e.x)/Math.max(d,1)*e.speed*dt;e.y+=(p.y-e.y)/Math.max(d,1)*e.speed*dt}if(d<p.r+18){hp-=((e.boss?24:9)+difficulty*2)*dt;if(hp<=0)void finish(false)}}for(const l of loot){const d=Math.hypot(p.x-l.x,p.y-l.y);if(d<(upgrades.has('magnet')?170:36)){if(l.kind==='shard')portalShards++;else coins+=8;l.x=-999}}loot=loot.filter(l=>l.x>-100);particles.forEach(x=>{x.life-=dt;x.r+=20*dt});particles=particles.filter(x=>x.life>0);camera.x+=(p.x-canvas.width/2-camera.x)*Math.min(1,dt*6);camera.y+=(p.y-canvas.height/2-camera.y)*Math.min(1,dt*6);camera.x=Math.max(0,Math.min(world.w-canvas.width,camera.x));camera.y=Math.max(0,Math.min(world.h-canvas.height,camera.y));const done=quests().every(q=>q.v>=q.g);if(done)void finish(true);syncHud();}
function draw(){ctx.clearRect(0,0,canvas.width,canvas.height);ctx.save();ctx.translate(-camera.x,-camera.y);for(const z of zones){ctx.fillStyle=z.c;ctx.fillRect(z.x,z.y,z.w,z.h);ctx.strokeStyle='rgba(247,201,72,.18)';ctx.lineWidth=4;ctx.strokeRect(z.x+2,z.y+2,z.w-4,z.h-4);ctx.fillStyle='rgba(255,255,255,.08)';ctx.font='900 44px system-ui';ctx.fillText(z.n,z.x+30,z.y+70)}for(let x=80;x<world.w;x+=180)for(let y=100;y<world.h;y+=170){ctx.fillStyle=`rgba(49,255,99,${.025+.02*Math.sin(worldTime+x)})`;ctx.fillRect(x,y,70,70)}for(const l of loot){ctx.beginPath();ctx.fillStyle=l.kind==='shard'?'#31ff63':'#f7c948';ctx.arc(l.x,l.y,9,0,Math.PI*2);ctx.fill();ctx.shadowBlur=20;ctx.shadowColor=ctx.fillStyle}ctx.shadowBlur=0;for(const e of enemies){ctx.beginPath();ctx.fillStyle=e.boss?'#f7c948':'#892cff';ctx.arc(e.x,e.y,e.boss?32:20,0,Math.PI*2);ctx.fill();ctx.strokeStyle=e.boss?'#fff1a0':'#d9a0ff';ctx.lineWidth=4;ctx.stroke();ctx.fillStyle='#190816';ctx.fillRect(e.x-28,e.y-(e.boss?48:36),56,5);ctx.fillStyle='#31ff63';ctx.fillRect(e.x-28,e.y-(e.boss?48:36),56*Math.max(0,e.hp/e.max),5)}for(const pt of particles){ctx.globalAlpha=pt.life;ctx.beginPath();ctx.fillStyle='#31ff63';ctx.arc(pt.x,pt.y,pt.r,0,Math.PI*2);ctx.fill()}ctx.globalAlpha=1;ctx.save();ctx.translate(p.x,p.y);ctx.rotate(p.dir);ctx.beginPath();ctx.fillStyle='#f7c948';ctx.arc(0,0,p.r,0,Math.PI*2);ctx.fill();ctx.strokeStyle='#31ff63';ctx.lineWidth=7;ctx.stroke();ctx.fillStyle='#020402';ctx.fillRect(5,-4,22,8);ctx.restore();ctx.restore()}
function loop(t:number){if(!running)return;const dt=Math.min(.035,(t-last)/1000||0);last=t;update(dt);draw();requestAnimationFrame(loop)}
async function finish(won:boolean){if(!running)return;running=false;const final=Math.max(0,score+(won?1500+mission*300:200)+kills*45+portalShards*80);best=Math.max(best,final);if(won){missionsDone++;mission++;xp+=Math.floor(final/12);level=1+Math.floor(xp/1800);toast('MISSION COMPLETE');$('#log').textContent=`PORTAL CLEARED • ${final.toLocaleString()} SCORE • Mission ${mission} unlocked.`}else{xp+=Math.floor(final/25);toast('WORLD DEFEAT');$('#log').textContent=`EXPEDITION ENDED • ${final.toLocaleString()} SCORE`;}save();renderSide();syncHud();if(user)await persist(final,won)}
async function persist(final:number,won:boolean){const duration=Math.floor((Date.now()-started)/1000);await supabase.from('game_sessions').insert({game_id:GAME_ID,game_slug:'avatar-free-roam',user_id:user.id,username:profile?.username,display_name:profile?.display_name,ended_at:new Date().toISOString(),duration_seconds:duration,result:won?'MISSION_COMPLETE':'DEFEAT',score:final,platform_type:'web',metadata:{mode,mission,level,kills,portalShards}});await supabase.from('game_scores').insert({game_id:GAME_ID,user_id:user.id,score:final,score_type:'rpg_world_score',metadata:{mission,level,mode}});await supabase.from('game_player_progress').upsert({game_id:GAME_ID,user_id:user.id,game_slug:'avatar-free-roam',xp,total_playtime_seconds:duration,progress_data:{mission,level,best,missionsDone,coins,portalShards,upgrades:[...upgrades],position:{x:p.x,y:p.y}},updated_at:new Date().toISOString()},{onConflict:'game_id,user_id'}).then(()=>{});}
async function auth(){const {data:{user:u}}=await supabase.auth.getUser();user=u;if(!u)return;const {data}=await supabase.from('profiles').select('username,display_name,avatar_url,rich_points,rich_level').eq('id',u.id).maybeSingle();profile=data;$('#name').textContent=(data?.display_name||data?.username||'PORTAL RONIN').toUpperCase();$('#net').textContent='ONLINE SYNC READY';loadLeaders()}
async function loadLeaders(){const {data}=await supabase.from('game_scores').select('score,profiles(display_name,username)').eq('game_id',GAME_ID).order('score',{ascending:false}).limit(6);$('#leaders').innerHTML=(data||[]).map((x:any,i)=>`<div class="afr-player"><span>${i+1}. ${x.profiles?.display_name||x.profiles?.username||'PLAYER'}</span><b>${Number(x.score).toLocaleString()}</b></div>`).join('')||'<p class="afr-log">Be the first world legend.</p>'}
async function createRoom(){if(!user)return toast('TAP IN REQUIRED');const code=Math.random().toString(36).slice(2,8).toUpperCase();const {data,error}=await supabase.from('game_rooms').insert({game_id:GAME_ID,room_code:code,host_user_id:user.id,status:'waiting',max_players:8,room_type:'realtime_rpg',settings:{mission,difficulty}}).select().single();if(error)return toast('ROOM UNAVAILABLE');room=data;($('#roomCode') as HTMLInputElement).value=code;connectRoom();}
async function joinRoom(){if(!user)return toast('TAP IN REQUIRED');const code=($('#roomCode') as HTMLInputElement).value.trim().toUpperCase();const {data}=await supabase.from('game_rooms').select('*').eq('game_id',GAME_ID).eq('room_code',code).maybeSingle();if(!data)return toast('ROOM NOT FOUND');room=data;connectRoom()}
function connectRoom(){channel?.unsubscribe();channel=supabase.channel(`afr-${room.room_code}`,{config:{broadcast:{self:false},presence:{key:user.id}}}).on('broadcast',{event:'position'},({payload})=>{if(payload?.id!==user.id)$('#roomState').textContent=`Party active • ${payload.name||'Player'} exploring zone ${payload.zone||'world'}`}).subscribe(async status=>{if(status==='SUBSCRIBED'){await channel.track({name:profile?.display_name||profile?.username||'Player'});$('#roomState').textContent=`ROOM ${room.room_code} • REALTIME CONNECTED`;setInterval(()=>channel.send({type:'broadcast',event:'position',payload:{id:user.id,name:profile?.display_name||profile?.username,x:p.x,y:p.y,zone:$('#zone').textContent}}),650)}})}
window.addEventListener('keydown',e=>{keys.add(e.key.toLowerCase());if(e.key===' ')attack(false);if(e.key.toLowerCase()==='q')attack(true);if(e.key.toLowerCase()==='shift')dash()});window.addEventListener('keyup',e=>keys.delete(e.key.toLowerCase()));document.querySelectorAll<HTMLButtonElement>('[data-act]').forEach(b=>{const a=b.dataset.act!;const k={left:'arrowleft',right:'arrowright',up:'arrowup',down:'arrowdown'}[a];if(k){b.onpointerdown=()=>keys.add(k);b.onpointerup=b.onpointerleave=()=>keys.delete(k)}else b.onclick=()=>a==='attack'?attack(false):a==='power'?attack(true):dash()});$('#start').onclick=start;$('#createRoom').onclick=createRoom;$('#joinRoom').onclick=joinRoom;window.addEventListener('beforeunload',()=>{save();channel?.unsubscribe()});load();void auth();draw();
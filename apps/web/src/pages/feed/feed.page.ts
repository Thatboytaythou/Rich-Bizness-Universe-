import { getAuthSnapshot } from '../../core/auth/auth-store';
import { supabase } from '../../core/supabase/client';
import './feed.css';

type Row=Record<string,any>;
type Channel=ReturnType<typeof supabase.channel>;
const esc=(v:unknown)=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]??c));
const safe=(v:unknown)=>{try{const u=new URL(String(v||''),location.origin);return ['http:','https:'].includes(u.protocol)?u.href:'';}catch{return'';}};
const ago=(v:string)=>{const s=Math.max(1,Math.floor((Date.now()-new Date(v).getTime())/1000));if(s<60)return`${s}s`;if(s<3600)return`${Math.floor(s/60)}m`;if(s<86400)return`${Math.floor(s/3600)}h`;return`${Math.floor(s/86400)}d`;};
const lane=(v:string)=>({feed:'FEED',gallery:'GALLERY',watch:'WATCH',music:'MUSIC',podcast:'PODCAST',radio:'RADIO',sports:'SPORTS',gaming:'GAMING',store:'STORE',live:'LIVE',meta:'META'}[v]||String(v||'FEED').toUpperCase());

export async function mount():Promise<void>{
 const root=document.querySelector<HTMLElement>('#app');if(!root)throw new Error('Missing #app');if(root.dataset.mounted==='feedOwner')return;root.dataset.mounted='feedOwner';
 const auth=getAuthSnapshot(),user=auth.user,userId=user?.id??null;const session=localStorage.getItem('rb_feed_session')||crypto.randomUUID();localStorage.setItem('rb_feed_session',session);
 root.innerHTML=`<main class="feed-shell"><div class="feed-wrap"><header class="feed-top"><a href="/portal.html">←</a><div><small>RICH BIZNESS SOCIAL UNIVERSE</small><h1>RICH FEED</h1></div><nav><a href="/search.html">SEARCH</a><a href="/upload.html?route=feed">UPLOAD</a><a href="/profile.html${userId?`?id=${encodeURIComponent(userId)}`:''}">PROFILE</a></nav></header><section class="feed-command"><div><span>SERVER-RANKED SOCIAL SIGNAL</span><h2>EVERY UNIVERSE. ONE FEED.</h2><p>Creator drops, live moments, music, sports, games, stores, worlds and visual culture ranked through one connected social engine.</p></div><div class="feed-command-links"><a href="/live.html">WE LIT 🔥</a><a href="/watch.html">WE 🔥 📺</a><a href="/messages.html">RICH-DM</a></div></section><section id="feedMetrics" class="feed-metrics"></section><div class="feed-layout"><section class="feed-main"><form id="composer" class="feed-composer"><div class="feed-composer-head"><div class="feed-avatar">RB</div><div><strong>${user?'RICH MEMBER':'PUBLIC VIEWER'}</strong><small>${user?'SERVER IDENTITY ACTIVE':'TAP IN TO POST'}</small></div></div><textarea id="postBody" maxlength="4000" placeholder="Drop something into the universe..." ${user?'':'disabled'}></textarea><div class="feed-composer-actions"><div><a class="feed-button" href="${user?'/upload.html?route=feed':'/tap-in.html?next=%2Ffeed.html'}">ADD MEDIA</a><select id="postSection" class="feed-button" ${user?'':'disabled'}><option value="feed">FEED</option><option value="gallery">GALLERY</option><option value="watch">WATCH</option><option value="music">MUSIC</option><option value="podcast">PODCAST</option><option value="radio">RADIO</option><option value="sports">SPORTS</option><option value="gaming">GAMING</option><option value="store">STORE</option><option value="live">LIVE</option><option value="meta">META</option></select></div><button class="feed-button primary">${user?'DROP POST':'TAP IN'}</button></div></form><div id="tabs" class="feed-tabs"></div><div id="posts"></div><p id="status" class="feed-status"></p></section><aside class="feed-side"><section class="feed-side-card"><h3>UNIVERSE LANES</h3>${[['LIVE','/live.html','◉'],['WATCH','/watch.html','▻'],['MUSIC','/music.html','♪'],['PODCAST','/podcast.html','◍'],['RADIO','/radio.html','◉'],['SPORTS','/sports.html','🏆'],['GAMING','/gaming.html','🎮'],['STORE','/store.html','🛒']].map(([n,h,i])=>`<a href="${h}"><span>${n}</span><b>${i}</b></a>`).join('')}</section></aside></div><div id="feedModal" class="feed-modal" hidden><article class="feed-modal-card"><button id="feedModalClose" class="feed-modal-close">×</button><div id="feedModalPost"></div><section class="feed-comments open"><div id="feedModalComments" class="feed-comment-list"></div><form id="feedModalForm" class="feed-comment-form"><input maxlength="2000" placeholder="Add to the conversation..." ${user?'':'disabled'}><button class="feed-button primary">${user?'POST':'TAP IN'}</button></form></section></article></div></div></main>`;
 const postsEl=root.querySelector<HTMLElement>('#posts')!,tabsEl=root.querySelector<HTMLElement>('#tabs')!,metrics=root.querySelector<HTMLElement>('#feedMetrics')!,status=root.querySelector<HTMLElement>('#status')!,modal=root.querySelector<HTMLElement>('#feedModal')!,modalPost=root.querySelector<HTMLElement>('#feedModalPost')!,modalComments=root.querySelector<HTMLElement>('#feedModalComments')!,modalForm=root.querySelector<HTMLFormElement>('#feedModalForm')!;
 let posts:Row[]=[],active:Row|null=null,filter=new URLSearchParams(location.search).get('section')||'all',channel:Channel|null=null,commentChannel:Channel|null=null,disposed=false,refreshTimer=0;
 const msg=(m:string)=>{status.textContent=m;setTimeout(()=>{if(status.textContent===m)status.textContent='';},2600);};const authRequired=()=>{if(userId)return true;location.assign(`/tap-in.html?next=${encodeURIComponent(location.pathname+location.search)}`);return false;};
 const media=(p:Row)=>{const src=safe(p.media_url||p.file_url||p.thumbnail_url||p.cover_url);if(!src)return'';const t=String(p.media_type||p.post_type||'').toLowerCase();if(t.includes('video')||/\.(mp4|webm|mov)(\?|$)/i.test(src))return`<video class="feed-media" controls playsinline preload="metadata" poster="${esc(safe(p.thumbnail_url||p.cover_url))}" src="${esc(src)}"></video>`;if(t.includes('audio'))return`<audio class="feed-media" controls preload="metadata" src="${esc(src)}"></audio>`;return`<img class="feed-media" loading="lazy" src="${esc(src)}" alt="${esc(p.title||'Feed media')}">`;};
 const card=(p:Row,compact=false)=>`<article class="feed-card ${p.is_pinned?'pinned':''} ${p.is_featured?'featured':''}" data-post="${esc(p.id)}"><header class="feed-card-head"><img src="${esc(p.avatar_url||'/brand/icons/profile-placeholder.svg')}" alt=""><div><a href="/profile.html?id=${encodeURIComponent(p.user_id)}"><strong>${esc(p.display_name||p.username||'Rich Creator')}</strong></a><span>@${esc(p.username||'member')} · ${ago(p.created_at)}${p.rank_title?` · ${esc(p.rank_title)}`:''}</span></div><em>${p.is_pinned?'PINNED':p.is_featured?'FEATURED':lane(p.section)}</em></header><div class="feed-card-copy">${p.title?`<h2>${esc(p.title)}</h2>`:''}${p.body?`<p>${esc(p.body)}</p>`:''}</div>${media(p)}<div class="feed-card-stats"><span>${Number(p.like_count||0).toLocaleString()} likes</span><span>${Number(p.comment_count||0).toLocaleString()} comments</span><span>${Number(p.repost_count||0).toLocaleString()} reposts</span><span>${Number(p.view_count||0).toLocaleString()} views</span></div>${compact?'':`<div class="feed-card-actions"><button data-action="toggle_like" class="${p.liked?'active':''}">${p.liked?'♥ LIKED':'♡ LIKE'}</button><button data-comments>◌ COMMENT</button><button data-action="toggle_repost" class="${p.reposted?'active':''}">${p.reposted?'↻ REPOSTED':'↻ REPOST'}</button><button data-action="toggle_save" class="${p.saved?'active':''}">${p.saved?'◆ SAVED':'◇ SAVE'}</button><button data-follow class="${p.following?'active':''}">${p.following?'✓ FOLLOWING':'+ FOLLOW'}</button><a href="/messages.html?share=${encodeURIComponent(p.id)}">✦ SHARE</a></div>`}</article>`;
 const render=()=>{postsEl.innerHTML=posts.length?posts.map(p=>card(p)).join(''):'<div class="feed-empty">No drops in this lane yet.</div>';postsEl.querySelectorAll<HTMLElement>('[data-post]').forEach(node=>{const p=posts.find(x=>String(x.id)===node.dataset.post)!;node.querySelectorAll<HTMLButtonElement>('[data-action]').forEach(b=>b.onclick=()=>void act(b.dataset.action!,p.id));const c=node.querySelector<HTMLButtonElement>('[data-comments]');if(c)c.onclick=()=>void open(p);const f=node.querySelector<HTMLButtonElement>('[data-follow]');if(f)f.onclick=()=>void act('toggle_follow',p.user_id);});};
 const renderTabs=()=>{const lanes=['all','feed','gallery','watch','music','podcast','radio','live','sports','gaming','store','meta'];tabsEl.innerHTML=lanes.map(v=>`<button class="feed-tab ${filter===v?'active':''}" data-filter="${v}">${v==='all'?'FOR YOU':v.toUpperCase()}</button>`).join('');tabsEl.querySelectorAll<HTMLButtonElement>('[data-filter]').forEach(b=>b.onclick=()=>{filter=b.dataset.filter||'all';const u=new URL(location.href);filter==='all'?u.searchParams.delete('section'):u.searchParams.set('section',filter);history.replaceState({},'',u);renderTabs();void load();});};
 const load=async()=>{const {data,error}=await supabase.rpc('rb_feed_snapshot',{p_section:filter,p_limit:100,p_post_id:active?.id||null});if(error)return msg(error.message);const s=(data||{}) as Row;posts=(s.posts||[]) as Row[];const m=s.metrics||{};metrics.innerHTML=`<article><small>POSTS</small><strong>${Number(m.posts||0).toLocaleString()}</strong></article><article><small>CREATORS</small><strong>${Number(m.creators||0).toLocaleString()}</strong></article><article><small>LIKES</small><strong>${Number(m.likes||0).toLocaleString()}</strong></article><article><small>VIEWS</small><strong>${Number(m.views||0).toLocaleString()}</strong></article>`;render();if(active){active=posts.find(p=>p.id===active?.id)||active;modalPost.innerHTML=card(active,true);modalComments.innerHTML=((s.comments||[]) as Row[]).map(c=>`<article class="feed-comment"><strong>${esc(c.display_name||c.username||'Rich Member')}</strong><small>${ago(c.created_at)}</small><p>${esc(c.body)}</p></article>`).join('')||'<div class="feed-empty">Start the conversation.</div>';}};
 const act=async(action:string,id:string,payload:Row={})=>{if(action!=='view'&&!authRequired())return;const {error}=await supabase.rpc('rb_feed_action',{p_action:action,p_payload:{...payload,post_id:id,session_id:session}});if(error)return msg(error.message);await load();};
 const open=async(p:Row)=>{active=p;modal.hidden=false;document.body.style.overflow='hidden';await load();if(commentChannel)await supabase.removeChannel(commentChannel);commentChannel=supabase.channel(`feed-comments:${p.id}`).on('postgres_changes',{event:'*',schema:'public',table:'feed_comments',filter:`post_id=eq.${p.id}`},()=>void load()).subscribe();};
 const close=async()=>{modal.hidden=true;document.body.style.overflow='';active=null;if(commentChannel){await supabase.removeChannel(commentChannel);commentChannel=null;}};
 root.querySelector<HTMLButtonElement>('#feedModalClose')!.onclick=()=>void close();modal.onclick=e=>{if(e.target===modal)void close();};window.onkeydown=e=>{if(e.key==='Escape'&&!modal.hidden)void close();};
 modalForm.onsubmit=async e=>{e.preventDefault();if(!active||!authRequired())return;const input=modalForm.querySelector<HTMLInputElement>('input')!,body=input.value.trim();if(!body)return;await act('comment',active.id,{body});input.value='';};
 root.querySelector<HTMLFormElement>('#composer')!.onsubmit=async e=>{e.preventDefault();if(!authRequired())return;const body=root.querySelector<HTMLTextAreaElement>('#postBody')!.value.trim(),section=root.querySelector<HTMLSelectElement>('#postSection')!.value;if(!body)return msg('WRITE SOMETHING FIRST');const {error}=await supabase.rpc('rb_feed_action',{p_action:'create_post',p_payload:{body,section}});if(error)return msg(error.message);root.querySelector<HTMLTextAreaElement>('#postBody')!.value='';msg('POST LIVE');await load();};
 const observer=new IntersectionObserver(es=>{for(const e of es){if(e.isIntersecting&&e.intersectionRatio>.55){const id=(e.target as HTMLElement).dataset.post;if(id){void act('view',id);observer.unobserve(e.target);}}}},{threshold:[.55]});
 renderTabs();await load();postsEl.querySelectorAll<HTMLElement>('[data-post]').forEach(n=>observer.observe(n));channel=supabase.channel('rich-feed-owner').on('postgres_changes',{event:'*',schema:'public',table:'feed_posts'},()=>{clearTimeout(refreshTimer);refreshTimer=window.setTimeout(()=>void load(),180);}).subscribe();
 const cleanup=()=>{if(disposed)return;disposed=true;observer.disconnect();clearTimeout(refreshTimer);document.body.style.overflow='';root.querySelectorAll('video,audio').forEach(m=>{const x=m as HTMLMediaElement;x.pause();x.removeAttribute('src');x.load();});if(channel)void supabase.removeChannel(channel);if(commentChannel)void supabase.removeChannel(commentChannel);};window.addEventListener('pagehide',cleanup,{once:true});window.addEventListener('beforeunload',cleanup,{once:true});
}

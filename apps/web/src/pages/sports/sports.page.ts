import { getAuthSnapshot } from '../../core/auth/auth-store';
import { supabase } from '../../core/supabase/client';
import '../../styles/deep-sections.css';
import '../../styles/sports-arena.css';

type Row=Record<string,any>;
type Snapshot={posts?:Row[];picks?:Row[];broadcasts?:Row[];leagues?:Row[];profile?:Row;metrics?:Row};
const esc=(v:unknown)=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]??c));
const date=(v:unknown)=>v?new Intl.DateTimeFormat('en-US',{month:'short',day:'numeric',hour:'numeric',minute:'2-digit'}).format(new Date(String(v))):'';
const safeRoute=(v:unknown)=>{try{const u=new URL(String(v||'/live.html'),location.origin);return u.origin===location.origin&&['http:','https:'].includes(u.protocol)?`${u.pathname}${u.search}${u.hash}`:'/live.html';}catch{return'/live.html';}};
const mediaBox=(url:unknown,label:string)=>url?`<img src="${esc(url)}" alt="" loading="lazy">`:`<div class="sports-media-empty">${esc(label)}</div>`;

export async function mount():Promise<void>{
 const root=document.querySelector<HTMLElement>('#app');if(!root)throw new Error('Missing #app');if(root.dataset.sportsOwner==='mounted')return;root.dataset.sportsOwner='mounted';
 const user=getAuthSnapshot().user;
 root.innerHTML=`<main class="deep-shell sports-arena"><div class="deep-wrap"><header class="deep-top"><a href="/portal.html">←</a><div><p>RICH BIZNESS SPORTS NETWORK</p><h1>Arena</h1></div><span class="deep-live">LIVE DATA</span></header><section class="deep-hero sports-hero"><div><small>HIGHLIGHTS • PICKS • BROADCASTS • TEAMS</small><h2>OWN THE MOMENT</h2><p>A cinematic sports universe for elite highlights, creator broadcasts, league conversation, premium picks, fan identity, and competition.</p><div class="deep-actions"><a class="deep-btn primary" href="/upload.html?route=sports">DROP HIGHLIGHT</a><button id="openPick" class="deep-btn">MAKE A PICK</button><a class="deep-btn" href="/live.html?category=sports">GO LIVE</a><a class="deep-btn" href="/watch.html?source=sports">WATCH SPORTS</a></div></div></section><nav id="tabs" class="deep-tabs"></nav><section id="stats" class="deep-stats"></section><section id="content"></section><p id="status" class="deep-status" role="status"></p></div><div id="pickModal" class="deep-modal" hidden><section class="deep-modal-card"><header><div><small>SPORTS XP EVENT</small><h2>MAKE A PICK</h2></div><button id="closePick" type="button">×</button></header><form id="pickForm" class="deep-form"><div class="deep-form-row"><input id="pickTitle" required maxlength="120" placeholder="Pick title"><input id="pickSport" required maxlength="60" placeholder="Sport"></div><div class="deep-form-row"><input id="pickTeam" maxlength="80" placeholder="Team"><input id="pickOpponent" maxlength="80" placeholder="Opponent"></div><textarea id="pickPrediction" required maxlength="1000" placeholder="Prediction and reasoning"></textarea><div class="deep-form-row"><input id="pickConfidence" type="number" min="1" max="100" value="70"><input id="pickTime" type="datetime-local"></div><button class="deep-btn primary">LOCK PICK</button></form></section></div><div id="talkModal" class="deep-modal" hidden><section class="deep-modal-card sports-talk-card"><header><div><small>ARENA CONVERSATION</small><h2 id="talkTitle">SPORTS TALK</h2></div><button id="closeTalk" type="button">×</button></header><div id="talkList" class="sports-talk-list"></div><form id="talkForm" class="sports-talk-form"><textarea id="talkBody" maxlength="1200" required placeholder="Add to the sports conversation..."></textarea><button class="deep-btn primary">POST COMMENT</button></form></section></div></main>`;
 const tabs=root.querySelector<HTMLElement>('#tabs')!,stats=root.querySelector<HTMLElement>('#stats')!,content=root.querySelector<HTMLElement>('#content')!,status=root.querySelector<HTMLElement>('#status')!,pickModal=root.querySelector<HTMLElement>('#pickModal')!,talkModal=root.querySelector<HTMLElement>('#talkModal')!,talkList=root.querySelector<HTMLElement>('#talkList')!;
 let lane='highlights',snapshot:Snapshot={},activePost:string|null=null,channel:ReturnType<typeof supabase.channel>|null=null,loading=false,queued=false,destroyed=false,refreshTimer:number|undefined;
 const lanes=[['highlights','HIGHLIGHTS'],['picks','PICKS'],['broadcasts','BROADCASTS'],['teams','LEAGUES + TEAMS'],['profile','MY SPORTS ID']];
 const setStatus=(v:string,error=false)=>{status.textContent=v;status.dataset.error=String(error);window.setTimeout(()=>{if(status.textContent===v)status.textContent='';},3000)};
 const action=async(name:string,payload:Row)=>{const{error}=await supabase.rpc('rb_sports_action',{p_action:name,p_payload:payload});if(error)throw error;};
 const openComments=async(postId:string,title:string)=>{activePost=postId;root.querySelector<HTMLElement>('#talkTitle')!.textContent=title||'SPORTS TALK';talkModal.hidden=false;document.body.style.overflow='hidden';const{data,error}=await supabase.from('sports_comments').select('id,username,display_name,body,created_at').eq('post_id',postId).order('created_at').limit(160);talkList.innerHTML=error?`<div class="deep-empty">${esc(error.message)}</div>`:(data??[]).map(r=>`<article><strong>${esc(r.display_name||r.username||'Rich Fan')}</strong><p>${esc(r.body)}</p><time>${date(r.created_at)}</time></article>`).join('')||'<div class="deep-empty">Start the arena conversation.</div>';talkList.scrollTop=talkList.scrollHeight;};
 const bind=()=>{content.querySelectorAll<HTMLButtonElement>('[data-react]').forEach(b=>b.onclick=async()=>{if(!user)return location.assign('/tap-in.html?next=%2Fsports.html');try{await action('reaction',{post_id:b.dataset.post,reaction:b.dataset.react});await load();}catch(e){setStatus(e instanceof Error?e.message:'Reaction failed',true);}});content.querySelectorAll<HTMLButtonElement>('[data-talk]').forEach(b=>b.onclick=()=>void openComments(b.dataset.post!,b.dataset.title||'Sports Talk'));};
 const renderTabs=()=>{tabs.innerHTML=lanes.map(([k,l])=>`<button class="deep-tab ${lane===k?'active':''}" data-lane="${k}">${l}</button>`).join('');tabs.querySelectorAll<HTMLButtonElement>('[data-lane]').forEach(b=>b.onclick=()=>{lane=b.dataset.lane||'highlights';renderTabs();render();});};
 const highlightCard=(p:Row)=>{const m=p.media_url||p.thumbnail_url||p.cover_url;const art=m?(String(p.media_type||'').startsWith('video')?`<video src="${esc(m)}" controls playsinline preload="metadata"></video>`:mediaBox(m,'RB SPORTS')):mediaBox(null,'RB SPORTS');return`<article class="deep-card sports-highlight"><div class="deep-card-media wide">${art}<span>${esc(p.sport||'SPORTS')}</span></div><div class="deep-card-body"><h3>${esc(p.title||'Sports Drop')}</h3><p>${esc(p.body||`${p.team_name||''} ${p.league||''}`)}</p><div class="deep-card-meta"><span>◌ ${p.comment_count??0} · 🔥 ${p.reaction_count??0}</span><span>◉ ${p.view_count??0}</span></div><div class="sports-reaction-row"><button data-react="🔥" data-post="${p.id}">🔥</button><button data-react="💯" data-post="${p.id}">💯</button><button data-react="🏆" data-post="${p.id}">🏆</button><button data-talk data-post="${p.id}" data-title="${esc(p.title||'Sports Talk')}">COMMENT</button></div><div class="deep-card-actions"><a href="/profile.html?id=${p.user_id}">CREATOR</a><a href="/watch.html?source=sports_clip&id=${p.id}">WATCH</a></div></div></article>`;};
 const pickCard=(p:Row)=>`<article class="deep-card sports-pick-card"><div class="deep-card-body"><span class="deep-badge ${p.result==='loss'?'danger':p.result==='win'?'':'warn'}">${esc((p.result||p.status||'OPEN').toUpperCase())}</span><h3>${esc(p.title)}</h3><p>${esc(p.prediction||'')}</p><div class="deep-card-meta"><span>${esc(p.team_name||'')} vs ${esc(p.opponent||'')}</span><span>${p.confidence??0}%</span></div><div class="sports-confidence"><i style="--confidence:${Math.min(100,Math.max(0,Number(p.confidence??0)))}%"></i></div><div class="deep-card-actions"><a href="/profile.html?id=${p.user_id}">${esc(p.display_name||'PICKER')}</a></div></div></article>`;
 const broadcastCard=(b:Row)=>`<article class="deep-card ${b.status==='live'?'sports-broadcast-live':''}"><div class="deep-card-media wide">${mediaBox(b.cover_url,'LIVE SPORTS')}<span>${esc((b.status||'SCHEDULED').toUpperCase())}</span></div><div class="deep-card-body"><h3>${esc(b.title)}</h3><p>${esc(b.description||`${b.team_name||''} ${b.league||''}`)}</p><div class="deep-card-meta"><span>${date(b.scheduled_for)}</span><span>◉ ${b.viewer_count??0}</span></div><div class="deep-card-actions"><a href="${esc(safeRoute(b.stream_url||b.replay_url))}">${b.status==='live'?'ENTER LIVE':'WATCH'}</a></div></div></article>`;
 const leagueCard=(l:Row)=>`<article class="deep-card sports-league-card"><div class="deep-card-media wide">${mediaBox(l.cover_url||l.logo_url,l.sport||'LEAGUE')}<span>${esc(l.sport||'LEAGUE')}</span></div><div class="deep-card-body"><h3>${esc(l.title)}</h3><p>Follow coverage, teams, broadcasts, picks, and highlights.</p></div></article>`;
 const render=()=>{const posts=snapshot.posts??[],picks=snapshot.picks??[],broadcasts=snapshot.broadcasts??[],leagues=snapshot.leagues??[],profile=snapshot.profile??{},m=snapshot.metrics??{};stats.innerHTML=`<article><small>HIGHLIGHTS</small><strong>${Number(m.highlights??posts.length).toLocaleString()}</strong></article><article><small>ACTIVE PICKS</small><strong>${Number(m.active_picks??0).toLocaleString()}</strong></article><article><small>BROADCASTS</small><strong>${Number(m.broadcasts??broadcasts.length).toLocaleString()}</strong></article><article><small>SPORTS XP</small><strong>${Number(m.sports_xp??profile.points??0).toLocaleString()}</strong></article>`;
 if(lane==='highlights')content.innerHTML=`<section class="deep-grid">${posts.map(highlightCard).join('')||'<div class="deep-empty">No highlights yet.</div>'}</section>`;
 else if(lane==='picks')content.innerHTML=`<section class="deep-grid">${picks.map(pickCard).join('')||'<div class="deep-empty">No picks locked yet.</div>'}</section>`;
 else if(lane==='broadcasts')content.innerHTML=`<section class="deep-grid">${broadcasts.map(broadcastCard).join('')||'<div class="deep-empty">No broadcasts scheduled.</div>'}</section>`;
 else if(lane==='teams')content.innerHTML=`<section class="deep-grid">${leagues.map(leagueCard).join('')||'<div class="deep-empty">League directory is ready for content.</div>'}</section>`;
 else content.innerHTML=Object.keys(profile).length?`<section class="deep-section sports-profile-panel"><header><div><small>CONNECTED SPORTS ID</small><h2>${esc(profile.display_name||profile.fan_tag||'Rich Fan')}</h2><p>${esc(profile.rank_title||'Rookie Fan')}</p></div><div class="deep-actions"><a class="deep-btn" href="/edit-profile.html">EDIT IDENTITY</a><a class="deep-btn" href="/creator.html">CREATOR HUB</a></div></header><div class="deep-stats"><article><small>FAVORITE SPORT</small><strong>${esc(profile.favorite_sport||'OPEN')}</strong></article><article><small>TEAM</small><strong>${esc(profile.favorite_team||'OPEN')}</strong></article><article><small>WINS</small><strong>${profile.win_count??0}</strong></article><article><small>STREAK</small><strong>${profile.pick_streak??0}</strong></article></div></section>`:'<div class="deep-empty">Create your Sports identity from Profile settings.</div>';bind();};
 const load=async()=>{if(loading){queued=true;return;}loading=true;try{const{data,error}=await supabase.rpc('rb_sports_snapshot',{p_limit:100});if(error)throw error;snapshot=(data??{}) as Snapshot;if(!destroyed)render();}catch(e){setStatus(e instanceof Error?e.message:'Sports sync failed',true);}finally{loading=false;if(queued&&!destroyed){queued=false;void load();}}};
 const scheduleLoad=()=>{window.clearTimeout(refreshTimer);refreshTimer=window.setTimeout(()=>void load(),180);};
 const close=(modal:HTMLElement)=>{modal.hidden=true;if(pickModal.hidden&&talkModal.hidden)document.body.style.overflow='';};
 root.querySelector<HTMLButtonElement>('#openPick')!.onclick=()=>{if(!user)return location.assign('/tap-in.html?next=%2Fsports.html');pickModal.hidden=false;document.body.style.overflow='hidden';};root.querySelector<HTMLButtonElement>('#closePick')!.onclick=()=>close(pickModal);root.querySelector<HTMLButtonElement>('#closeTalk')!.onclick=()=>close(talkModal);
 root.querySelector<HTMLFormElement>('#pickForm')!.onsubmit=async e=>{e.preventDefault();if(!user)return;const form=e.currentTarget;const button=form.querySelector<HTMLButtonElement>('button');if(button)button.disabled=true;try{await action('create_pick',{title:root.querySelector<HTMLInputElement>('#pickTitle')!.value.trim(),sport:root.querySelector<HTMLInputElement>('#pickSport')!.value.trim(),team_name:root.querySelector<HTMLInputElement>('#pickTeam')!.value.trim(),opponent:root.querySelector<HTMLInputElement>('#pickOpponent')!.value.trim(),prediction:root.querySelector<HTMLTextAreaElement>('#pickPrediction')!.value.trim(),confidence:Number(root.querySelector<HTMLInputElement>('#pickConfidence')!.value),game_time:root.querySelector<HTMLInputElement>('#pickTime')!.value||null});form.reset();close(pickModal);setStatus('PICK LOCKED — XP SYNCED');await load();}catch(err){setStatus(err instanceof Error?err.message:'Pick failed',true);}finally{if(button)button.disabled=false;}};
 root.querySelector<HTMLFormElement>('#talkForm')!.onsubmit=async e=>{e.preventDefault();if(!user||!activePost)return location.assign('/tap-in.html?next=%2Fsports.html');const box=root.querySelector<HTMLTextAreaElement>('#talkBody')!;try{await action('comment',{post_id:activePost,body:box.value.trim()});box.value='';await openComments(activePost,root.querySelector<HTMLElement>('#talkTitle')!.textContent||'Sports Talk');await load();}catch(err){setStatus(err instanceof Error?err.message:'Comment failed',true);}};
 renderTabs();await load();channel=supabase.channel('sports-arena-live').on('postgres_changes',{event:'*',schema:'public',table:'sports_posts'},scheduleLoad).on('postgres_changes',{event:'*',schema:'public',table:'sports_picks'},scheduleLoad).on('postgres_changes',{event:'*',schema:'public',table:'sports_broadcasts'},scheduleLoad).subscribe();
 const cleanup=()=>{if(destroyed)return;destroyed=true;window.clearTimeout(refreshTimer);document.body.style.overflow='';if(channel)void supabase.removeChannel(channel);};window.addEventListener('pagehide',cleanup,{once:true});window.addEventListener('beforeunload',cleanup,{once:true});
}
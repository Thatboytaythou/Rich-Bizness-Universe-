import { createArcadeGame } from '../shared/runtime/create-arcade-game';
createArcadeGame({ slug:'golf-green-gold', title:'Golf Green Gold', instruction:'Sink clean shots under pressure.', actionLabel:'TAKE SHOT', targetScore:900, timeLimitSeconds:45, pointsPerAction:75 });

import { createArcadeGame } from '../shared/runtime/create-arcade-game';
createArcadeGame({ slug:'portal-dash', title:'Portal Dash', instruction:'Chain portal jumps without breaking pace.', actionLabel:'DASH PORTAL', targetScore:1600, timeLimitSeconds:40, pointsPerAction:100 });

import { createArcadeGame } from '../shared/runtime/create-arcade-game';
createArcadeGame({ slug:'smoke-burst-arena', title:'Smoke Burst Arena', instruction:'Charge smoke bursts and control the arena.', actionLabel:'SMOKE BURST', targetScore:2000, timeLimitSeconds:45, pointsPerAction:125 });

import { createArcadeGame } from '../shared/runtime/create-arcade-game';
createArcadeGame({ slug:'treehouse-ride', title:'Treehouse Ride', instruction:'Drive the money road and reach the treehouse.', actionLabel:'HIT BOOST', targetScore:1700, timeLimitSeconds:45, pointsPerAction:85 });

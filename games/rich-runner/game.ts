import { createArcadeGame } from '../shared/runtime/create-arcade-game';
createArcadeGame({ slug:'rich-runner', title:'Rich Runner', instruction:'Keep the streak alive to reach the vault.', actionLabel:'CLEAR OBSTACLE', targetScore:1400, timeLimitSeconds:40, pointsPerAction:70 });

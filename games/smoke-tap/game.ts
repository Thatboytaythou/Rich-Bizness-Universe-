import { createArcadeGame } from '../shared/runtime/create-arcade-game';
createArcadeGame({ slug:'smoke-tap', title:'Smoke Tap', instruction:'Build the cloud meter with clean taps.', actionLabel:'SMOKE TAP', targetScore:1000, timeLimitSeconds:30, pointsPerAction:40 });

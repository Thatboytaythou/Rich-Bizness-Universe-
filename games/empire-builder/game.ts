import { createArcadeGame } from '../shared/runtime/create-arcade-game';
createArcadeGame({ slug:'empire-builder', title:'Empire Builder', instruction:'Stack assets and grow the empire.', actionLabel:'BUILD ASSET', targetScore:2000, timeLimitSeconds:60, pointsPerAction:125 });

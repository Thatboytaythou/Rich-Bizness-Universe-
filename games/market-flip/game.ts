import { createArcadeGame } from '../shared/runtime/create-arcade-game';
createArcadeGame({ slug:'market-flip', title:'Market Flip', instruction:'Flip deals and beat the market target.', actionLabel:'FLIP DEAL', targetScore:1750, timeLimitSeconds:55, pointsPerAction:125 });

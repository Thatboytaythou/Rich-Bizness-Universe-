import { createArcadeGame } from '../shared/runtime/create-arcade-game';
createArcadeGame({ slug:'bizness-party-room', title:'Bizness Party Room', instruction:'Build the party meter before time runs out.', actionLabel:'HYPE THE ROOM', targetScore:1200, timeLimitSeconds:45, pointsPerAction:75 });

import { createArcadeGame } from '../shared/runtime/create-arcade-game';
createArcadeGame({ slug:'dj-radio-run', title:'DJ Radio Run', instruction:'Keep the station energy climbing.', actionLabel:'DROP THE BEAT', targetScore:1600, timeLimitSeconds:45, pointsPerAction:80 });

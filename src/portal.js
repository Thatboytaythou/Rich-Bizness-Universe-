const canvas=document.querySelector('#portalCanvas');const ctx=canvas.getContext('2d');let w=0,h=0,dpr=1,t=0;function resize(){dpr=Math.min(devicePixelRatio||1,2);w=innerWidth;h=innerHeight;canvas.width=w*dpr;canvas.height=h*dpr;canvas.style.width=`${w}px`;canvas.style.height=`${h}px`;ctx.setTransform(dpr,0,0,dpr,0,0)}function frame(){t+=.006;ctx.clearRect(0,0,w,h);const cx=w*.72,cy=h*.49;for(let i=0;i<7;i++){const r=80+i*34+Math.sin(t*3+i)*8;ctx.beginPath();ctx.arc(cx,cy,r,0,Math.PI*2);ctx.strokeStyle=`rgba(${80+i*12},255,${70+i*4},${.16-i*.015})`;ctx.lineWidth=1+i*.25;ctx.stroke()}for(let i=0;i<90;i++){const a=i*.71+t*(i%3?1:-1);const r=130+(i%17)*22;const x=cx+Math.cos(a)*r;const y=cy+Math.sin(a*.83)*r*.52;ctx.fillStyle=`rgba(180,255,170,${.08+(i%5)*.025})`;ctx.fillRect(x,y,1.2,1.2)}requestAnimationFrame(frame)}addEventListener('resize',resize);resize();frame();document.querySelector('#portalCore')?.addEventListener('click',()=>{document.querySelector('#portalStatus').textContent='PORTAL ACTIVATED';document.body.classList.add('portal-active')});
